# Points to your backend (Railway/Render URL in production)
# In development the Vite proxy handles this automatically — leave it blank or set to /api
VITE_API_URL=http://localhost:4000/api

# Optional: set your deployed frontend URL for CORS
VITE_APP_URL=http://localhost:5173
